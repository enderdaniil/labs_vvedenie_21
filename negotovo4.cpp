#include <iostream>
#include <string>
#include <stdio.h>

using namespace std;


void top(char x)
{
	char arr[] = { 'а', 'ё', 'у', 'е', 'ы', 'о', 'э', 'я', 'и', 'ю' };
	for (int i = 0; i )
}


int main()
{
	setlocale(LC_ALL, "Russia");

	string a;
	cout << "Vvedite Stroku" << endl;
	getline(cin, a);

	int kolvo = 0;

	for (int i = 1; i < a.length(); i++)
	{
		
	}

	cout << "otvet = " << kolvo << endl;

	return 0;

}
